export default function Logo() {
  return (
    <a
      href="#top"
      className="origin-left text-sm font-medium tracking-[0.08em] transition-all duration-200 ease-out hover:scale-105 hover:text-accent sm:text-base"
    >
      TONSAMMLER
    </a>
  );
}
